#include <stdio.h>

int main(){

    int myNum;
    printf("type number :");
    scanf("%d", &myNum);

    if (myNum % 2 == 0)
        printf("%d is even.\n", myNum);
    else{
        printf("%d is odd.\n", myNum);
    }
    return 0;














}
