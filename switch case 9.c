#include <stdio.h>
    int main()
    {
        int x = 'b';
        switch (x)
        {
           case 'a':
              printf("Case A ");
           default:
              printf("Default");
        }
    }
